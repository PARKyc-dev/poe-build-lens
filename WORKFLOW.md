# Agent workflow

At the start of a task, read this file, `memory/CURRENT.md`, then relevant entries in `memory/DECISIONS.md`.

Before ending a task, update `memory/CURRENT.md` with the current goal, verification result, next action, and blockers. Add a decision entry only when a choice changes later implementation or analysis behaviour.
