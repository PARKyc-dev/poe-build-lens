spring:
  application:
    name: poe-lens
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

poe-lens:
  openai:
    enabled: true
    api-key: ${OPENAI_API_KEY:}
    model: gpt-5.6-luna
    daily-limit: 100
    daily-limit-zone: Asia/Seoul
