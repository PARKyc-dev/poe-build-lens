# 모노레포 및 React 전환

## 목표

Spring Boot API를 `api`로 분리하고 기존 정적 화면을 `web`의 Vite·React·TypeScript 앱으로 전환한다. `worker`는 향후 PoB 엔진을 위한 예약 디렉터리로 둔다.

## 범위

- 기존 Spring Boot 프로젝트를 `api`로 이동
- 기존 화면을 독립 React 앱으로 전환
- Vite 개발 프록시로 기존 분석 API 연결
- 루트 하네스와 README를 새 경계에 맞게 갱신
- `worker` 역할 문서 추가

Docker, PoB 워커 구현, 운영 배포, 새 분석 기능은 범위에서 제외한다.

## 완료 기준

- `web` 테스트와 프로덕션 빌드 성공
- `api` 전체 테스트와 Spring Boot 패키지 생성 성공
- 개발 프록시를 통한 실제 분석 흐름 확인
- 기존 API 계약과 분석 동작 유지

## 진행 상황

- 모노레포 경계와 React 전환 설계 승인
- 설계 문서 작성 및 구현 계획 전 사용자 검토 대기

## 검증 결과

- 구현 전이므로 없음

## 다음 행동

- 승인된 설계를 구현 계획으로 세분화
- 테스트 우선으로 파일 이동과 React 전환 수행

## 차단 요소

- 없음
