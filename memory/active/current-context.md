# Current Context

## Current Goal

실제 브라우저의 장시간 실행 환경에서 PoB 검사 완료 뒤 상세 화면으로 전환되는지 확인한다.

## In Progress

- 브라우저 Web Worker는 원본 PoB `HeadlessWrapper.lua`로 PoB 입력을 분석하고, React는 계산 결과를 상세 화면에 표시한다.

## Blockers

None.

## Next Actions

1. 사용자가 실행 중인 API를 재시작한 뒤 실제 브라우저에서 PoB 검사와 상세 화면 전환, 공격 동작·상호작용 설명을 확인한다.
2. 확인 결과를 기록하고 이 활성 문맥을 완료 처리한다.

## Relevant Files

- `web/src/pob/browserPob.ts`
- `web/src/pob/bridge.lua`

## Completed Work

- 2026-09-06: 공격·방어 기재의 긴 AI 문장을 SECTION 카드 구조로 변경했다. OpenAI 공격·방어 섹션은 빌드별 동적 `details`를 `step`, `interaction`, `condition`으로 구조화해 반환하며 서버가 종류와 기존 근거를 검증한다. 공격은 작동 과정·핵심 상호작용·보조젬 연결·운용 방식의 순서도와 카드로, 방어는 방어 자원·피해 경감·회피·막기·회복 및 저항 상호작용의 방어층·조건 카드로 표시한다. 저항 응답 병합 시 구조화 카드도 보존하며 모바일 순서도는 세로로 전환한다. API 전체 테스트·`bootJar`, 웹 화면 테스트 9개·프로덕션 빌드는 성공했고 웹 전체 Vitest는 기존과 동일하게 출력 없이 대기하여 종료했다.
- 2026-09-06: 공격·방어·버프·이동기 옆에 `저주/징표 기재`를 추가했다. PoB의 Hex는 `curse`, Mark는 `mark`로 명시적으로 구분하며, 플레이어 `buffList`에 나타나지 않는 Assassin's Mark 같은 적 대상 스킬도 활성 스킬 타입에서 직접 추출한다. 두 종류 모두 방어·버프 기재에서 제외해 독립 목록과 AI 설명으로 표시하고 기존 스킬 상세 팝업도 제공하며, 스킬젬 상세에서는 이동기 다음 우선순위로 정렬한다. 실제 Assassin's Mark PoB 통합 테스트, 화면 테스트 9개와 웹 프로덕션 빌드는 성공했고 웹 전체 Vitest는 기존 PoB 통합 테스트 대기 현상으로 종료했다.
- 2026-09-06: 스킬젬 상세 그룹을 `주력 공격 → 보조 공격 → 방어·가드 → 핵심 오라·버프 → 이동기 → 유틸리티 → 자동 발동` 우선순위로 정렬했다. 분류에는 PoB의 offence 역할, 방어 버프 종류·태그, mobility와 trigger 운용 사실을 사용하며 같은 분류에서는 기존 PoB 순서를 유지한다. 변경 대상 화면 테스트 9개와 웹 프로덕션 빌드는 성공했고 웹 전체 Vitest는 기존 PoB 통합 테스트 대기 현상으로 종료했다.
- 2026-09-06: 장비 상세 아래에 `스킬젬 상세` 영역을 추가했다. 활성 스킬젬별 카드 안에 실제 연결된 활성 보조 젬을 연결선과 함께 표시하고 각 젬의 레벨·퀄리티·품질 종류·각성 여부를 보여 준다. 연결된 보조 젬이 없거나 활성 스킬젬을 찾지 못한 경우도 안내한다. 변경 대상 화면 테스트 9개와 웹 프로덕션 빌드는 성공했다. 웹 전체 Vitest는 기존 PoB 통합 테스트 대기 현상으로 종료했다.
- 2026-09-06: 분석 대상이 없는 배열을 `maxItems: 0`으로 제한하면서 `items`에 불완전한 object 스키마를 사용해 OpenAI strict Structured Outputs 요청이 HTTP 400으로 거부되던 문제를 수정했다. 빈 배열의 item에도 전체 required 필드와 `additionalProperties: false`를 가진 정상 객체 스키마를 사용한다. 공식 OpenAI 문서에서 `maxItems` 지원과 strict 객체 제약을 확인했으며 재현 테스트, API 전체 테스트와 `bootJar`가 성공했다.
- 2026-09-06: 태그가 전부 비어 분석 대상 버프가 없는 Flicker Strike PoB에서 OpenAI 스키마가 임의의 buffName을 허용하고 서버 검증에서 실패하던 문제를 수정했다. 대상이 없는 공격·방어·버프 배열은 스키마에서 빈 배열로 강제하고, 대상 이름은 실제 PoB 목록으로 제한한다. 버프 evidence는 PoB 전체의 실제 출처와 태그를 사용할 수 있고 선택적 버프나 공격 섹션 누락은 전체 AI 응답을 폐기하지 않는다. 존재하지 않는 대상·근거 거부는 유지한다. 일반화 재현 테스트, API 전체 테스트와 `bootJar`가 성공했다.
- 2026-09-06: 두 번째 실제 OpenAI 응답에서 생명력 섹션이 함께 사용한 energy-shield·mana·life-regeneration 근거를 검증기가 거부하던 문제를 수정했다. defenceKind는 별도 필드로 검증하고, 방어 evidence는 실제 전체 방어 사실·버프 태그·빌드 출처와 허용된 구조 식별자만 스키마 단계부터 선택할 수 있게 제한했다. 저장된 실제 응답의 모든 방어 근거가 입력에 존재함을 확인했고 API 전체 테스트와 `bootJar`가 성공했다.
- 2026-09-06: 실제 OpenAI 방어 응답이 통합 저항 식별자(`resistances`, `resistance-interaction`)를 evidence에 포함했을 때 검증기가 이를 알 수 없는 근거로 거부하던 모순을 수정했다. 두 식별자는 해당 저항 섹션에서만 구조 표식으로 허용하고 실제 장비·패시브·전직 근거 검증은 유지한다. AI가 저항 내용을 여러 문단으로 나눠 반환해도 `저항 체계`와 `저항 핵심 상호작용` 각각 한 항목으로 합친다. 재현 테스트와 API 전체 테스트, `bootJar`가 성공했다.
- 2026-09-06: 빌드 요약과 공격·방어·버프 설명에서 주황색으로 강조한 전직 노드를 마우스로 가리키거나 키보드로 포커스하면, 기존 스킬 상세 표시와 같은 작은 팝업으로 해당 전직 효과를 보여 주도록 변경했다. 변경 대상 화면 테스트 9개와 웹 프로덕션 빌드는 성공했다. 웹 전체 Vitest는 기존과 동일하게 출력 없이 대기하여 종료했다.
- 2026-09-06: 방어 기재의 화염·냉기·번개·카오스 저항 설명을 `저항 체계` 하나로 통합하고, Valako·Tasalio처럼 저항 적용·최대치·피해 전환을 바꾸는 실제 효과가 있을 때 `저항 핵심 상호작용`을 선택적으로 생성하도록 AI 프롬프트·스키마·검증을 변경했다. 전직 노드는 쉼표 뒤 칭호를 포함한 PoB 전체 이름을 사용하며, 웹의 요약·상세 설명·전직 목록에서 실제 전직 이름을 주황색으로 강조한다. API 전체 테스트·`bootJar`, 웹 상세 화면 테스트·프로덕션 빌드는 성공했다. 웹 전체 Vitest는 기존과 동일하게 출력 없이 대기하여 종료했다.
- 2026-09-06: AI 빌드 설명의 가독성을 위해 `buildSummary`를 수치·세부 출처 나열 없는 3~5문장의 핵심 작동 구조로 제한했다. 공격의 동작·수치·보조젬·상호작용, 방어 수치·방어층·회복 조건, 버프 대상·효과·유지 조건은 각 상세 섹션에서 설명하고 결론을 먼저 쓰도록 프롬프트와 스키마 설명을 정리했다. 상세 문장의 줄바꿈은 웹에서 보존한다. API 전체 테스트·`bootJar`, 웹 상세 화면 테스트·프로덕션 빌드는 성공했다. 웹 전체 Vitest는 기존과 동일하게 출력 없이 대기하여 종료했다.
- 2026-09-06: 메인 화면에 오늘의 AI 분석 사용량을 `사용량/한도` 형식으로 표시했다. `/api/ai-usage`가 Supabase의 Asia/Seoul 오늘자 카운트와 `application.yaml`의 `poe-lens.openai.daily-limit` 값을 반환하며, 웹은 최초 진입과 분석 시도 후 이를 갱신한다. 실제 실행 중인 API에서 `used=1`, `limit=100` 응답을 확인했다. API 전체 테스트·`bootJar`, 웹 변경 대상 테스트·프로덕션 빌드는 성공했다. 웹 전체 Vitest는 기존과 동일하게 출력 없이 대기하여 종료했다.
- 2026-09-06: 실제 Righteous Fire/Fire Trap AI 응답이 HTTP 200으로 생성된 뒤 공격 evidence에 attackName이 반복되지 않았다는 이유로 거부되던 검증 오류를 수정했다. attackName은 별도 필드로 검증하고 evidence는 실제 사용한 출처만 허용한다. 방어 evidence에는 실제 빌드 출처를 허용하며 모든 방어 수치의 섹션 생성을 강제하지 않는다. 대신 응답 스키마의 defenceKind와 buffName을 실제 PoB 목록으로 제한한다. API 전체 테스트와 `bootJar`가 성공했다.
- 2026-09-05: AI 필수 분석 전환 뒤 더 이상 사용하지 않는 규칙 기반 빌드 요약·공격·방어·버프 문장 생성기와 fallback 계약을 삭제했다. 분석 서비스는 PoB 사실과 규칙으로 추출한 운용 흐름만 AI에 전달하며, AI 응답은 필수 요약·섹션·근거가 모두 검증되어야 반환된다. API 전체 테스트와 `bootJar`가 성공했다.
- 2026-09-05: API 운영 데이터베이스를 PostgreSQL 전용으로 전환하고 `DB_URL`, `DB_USERNAME`, `DB_PASSWORD`를 필수화했으며 H2는 테스트 전용으로 한정했다. OpenAI 요청은 Supabase의 `openai_daily_usage` 테이블에서 Asia/Seoul 날짜별로 원자적으로 집계하며 서비스 전체 기준 하루 100회까지만 외부 호출한다. 한도 초과 시 HTTP 429와 `금일 AI 분석 리미트에 도달했습니다.` 메시지를 반환하고 웹이 알림으로 표시한다. OpenAI 비활성화·호출 실패·응답 검증 실패에도 규칙 기반 문장을 반환하지 않는다. API 전체 테스트·`bootJar`, 웹 변경 대상 테스트·프로덕션 빌드는 성공했다. 웹 전체 Vitest는 기존과 동일하게 출력 없이 대기하여 1분 뒤 종료했다. 실제 Supabase 기동 검증에서는 pooler 호스트에 맞지 않는 `DB_USERNAME` 형식 때문에 인증이 거부되어 환경변수 수정이 필요하다.
- 2026-08-25: 일반화된 스킬 운용 분석은 스킬·장비·주얼·패시브·전직·버프의 수정자/효과에서 `operationFacts`를 추출해, 소비-획득 순환, 처치/명중/피격 조건, 유지·예약·재사용 대기시간·전환·강화 흐름만 근거와 함께 AI 서술에 전달한다. 생명력·저항·이동·방어도 계열과 `Condition:`·`Multiplier:`·PvP 항목은 제외하며, 근거 없는 아이템 효과·충전 획득·발동 인과관계를 추측하지 않는다. 공개 분석 응답과 웹 API 응답 타입은 유지했다. API 전체 테스트와 `bootJar`, 웹 프로덕션 빌드는 성공했다. 웹 전체 Vitest는 결과 없이 2분간 대기한 뒤 중단되어 별도 원인 조사가 필요하다.
- 2026-08-23: API DDD 가독성 리팩터링을 완료했다. `build.domain.analysis`에 공격·방어·버프·패시브·장비·성능 분석기와 요약 생성기를 분리하고, `BuildAnalysisServiceImpl`은 이들을 조합하는 유스케이스로 유지했다. OpenAI 연동은 `ai.service`의 요청 클라이언트·스키마·섹션 검증기·보정기로 분리했다. 공개 API와 웹 타입은 바꾸지 않았고 `utils` 또는 repository는 추가하지 않았다. API 전체 테스트와 `bootJar` 검증을 통과했다.
- 2026-08-23: 상세 화면의 요약 제목을 `빌드 메커니즘 요약`으로 바꾸고, 요약 문장을 네 칸 그리드가 아닌 전체 폭 카드로 표시했다. 효과 태그가 없는 버프는 AI 기재 결과에서도 제외한다.
