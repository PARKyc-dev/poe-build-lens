import type { BrowserInspectResult } from '../pob/browserPob'
import type { DetailTooltip, ShowDetailTooltip } from './detailTooltip'

const labels: Record<string, string> = { 'Weapon 1': '주무기', 'Weapon 2': '보조 무기', Helmet: '투구', 'Body Armour': '갑옷', Gloves: '장갑', Boots: '부츠', Amulet: '목걸이', 'Ring 1': '반지 1', 'Ring 2': '반지 2', Belt: '허리띠', 'Flask 1': '플라스크 1', 'Flask 2': '플라스크 2', 'Flask 3': '플라스크 3', 'Flask 4': '플라스크 4', 'Flask 5': '플라스크 5' }
const slots = Object.keys(labels)
const classes: Record<string, string> = { 'Weapon 1': 'weapon-1', 'Weapon 2': 'weapon-2', Helmet: 'helmet', 'Body Armour': 'body-armour', Gloves: 'gloves', Boots: 'boots', Amulet: 'amulet', 'Ring 1': 'ring-1', 'Ring 2': 'ring-2', Belt: 'belt', 'Flask 1': 'flask-1', 'Flask 2': 'flask-2', 'Flask 3': 'flask-3', 'Flask 4': 'flask-4', 'Flask 5': 'flask-5' }

export function EquipmentSection({ result, tooltip, onShow, onHide }: { result: BrowserInspectResult; tooltip: DetailTooltip | null; onShow: ShowDetailTooltip; onHide: () => void }) {
  const bySlot = new Map(result.equipment.map((item) => [item.slot, item]))
  const jewels = result.jewels.filter((item) => item.kind === 'jewel')
  const clusterJewels = result.jewels.filter((item) => item.kind === 'cluster')
  const tooltipFor = (item: Pick<BrowserInspectResult['equipment'][number], 'name' | 'modifiers'>, label: string, id: string) => ({ id, label, title: item.name, details: item.modifiers })
  return <section className="equipment-panel" aria-label="장비 상세"><p className="section-kicker">EQUIPMENT</p><h2>장비 상세</h2>{result.equipment.length === 0 && result.jewels.length === 0 ? <p>활성 장비 세트와 패시브 트리에 장착된 아이템이 없습니다.</p> : <><div className="equipment-stage"><ul className="equipment-layout">{slots.map((slot) => { const item = bySlot.get(slot); const label = labels[slot]; return <li key={slot} className={`equipment-slot ${classes[slot]} ${item ? `rarity-${item.rarity.toLowerCase()}` : 'empty'}`}>{item ? <button type="button" aria-label={`${label} 슬롯: ${item.name}`} aria-describedby={tooltip?.id === `equipment:${slot}` ? 'item-tooltip' : undefined} onMouseEnter={(event) => onShow(event, tooltipFor(item, label, `equipment:${slot}`))} onMouseLeave={onHide} onFocus={(event) => onShow(event, tooltipFor(item, label, `equipment:${slot}`))} onBlur={onHide}>{item.imageUrl ? <img src={item.imageUrl} alt="" /> : <span className="equipment-slot-label">{label}</span>}<strong>{item.name}</strong></button> : <span aria-label={`${label} 슬롯`} className="equipment-slot-label">{label}</span>}</li> })}</ul>{result.jewels.length > 0 && <div className="jewel-groups">{jewels.length > 0 && <section aria-label="주얼"><h3>주얼</h3><ul className="jewel-list">{jewels.map((item) => <li key={item.socket} className={`rarity-${item.rarity.toLowerCase()}`}><button type="button" aria-label={`주얼: ${item.name}`} aria-describedby={tooltip?.id === `jewel:${item.socket}` ? 'item-tooltip' : undefined} onMouseEnter={(event) => onShow(event, tooltipFor(item, '주얼', `jewel:${item.socket}`))} onMouseLeave={onHide} onFocus={(event) => onShow(event, tooltipFor(item, '주얼', `jewel:${item.socket}`))} onBlur={onHide}><strong>{item.name}</strong><span>{item.baseName}</span></button></li>)}</ul></section>}{clusterJewels.length > 0 && <section aria-label="군 주얼"><h3>군 주얼</h3><ul className="jewel-list">{clusterJewels.map((item) => <li key={item.socket} className={`rarity-${item.rarity.toLowerCase()}`}><button type="button" aria-label={`군 주얼: ${item.name}`} aria-describedby={tooltip?.id === `jewel:${item.socket}` ? 'item-tooltip' : undefined} onMouseEnter={(event) => onShow(event, tooltipFor(item, '군 주얼', `jewel:${item.socket}`))} onMouseLeave={onHide} onFocus={(event) => onShow(event, tooltipFor(item, '군 주얼', `jewel:${item.socket}`))} onBlur={onHide}><strong>{item.name}</strong><span>{item.baseName}</span></button></li>)}</ul></section>}</div>}</div><ul className="equipment-details">{result.equipment.map((item) => <li key={item.slot} className={`rarity-${item.rarity.toLowerCase()}`}><small>{labels[item.slot] ?? item.slot}</small><strong>{item.name}</strong>{item.baseName && <span>{item.baseName}</span>}{item.modifiers.map((modifier) => <em key={modifier}>{modifier}</em>)}</li>)}{result.jewels.map((item) => <li key={item.socket} className={`rarity-${item.rarity.toLowerCase()}`}><small>{item.kind === 'cluster' ? '군 주얼' : '주얼'}</small><strong>{item.name}</strong>{item.baseName && <span>{item.baseName}</span>}{item.modifiers.map((modifier) => <em key={modifier}>{modifier}</em>)}</li>)}</ul></>}</section>
}
